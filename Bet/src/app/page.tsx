// Placeholder root page for Task 1. The marketing home ships in Task 14
// (SPEC §3.1); this just proves the app boots on the dark surface tokens.
export default function Home() {
  return <main />;
}
