# Secret used to sign the demo session JWT (Task 4). In development, a
# hardcoded fallback is used if this is unset (with a loud console warning) —
# never rely on that fallback outside local development.
AUTH_SECRET=
