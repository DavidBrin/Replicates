import { HomeScreen } from "@/components/home/home-screen";

/** Reached by ending or declining the call — the app's only way into options. */
export default function HomePage() {
  return <HomeScreen />;
}
