/**
 * Placeholder so `src/domain/` exists in git and lint has something to walk.
 * Pure logic only, no React/Next imports — replace as domain modules land.
 */
export function placeholder(): true {
  return true;
}
